name: test

on:
  push:
    branches:
      - main 
env:
  SALUDO: "saludo global"
  CMD: "es un CMD"

jobs:
  j1:
    runs-on: ubuntu-latest
    env:
     LOCAL_J1: "var local-j1"
    steps:
      - name: hola desde test
        run: |
         echo "hola bienvenido a test"
         echo "esto es un ${{ env.SALUDO }}"
         echo "local var ${{ env.LOCAL_J1 }}"
          
  j2:
    runs-on: ubuntu-latest
    env:
      LOCAL_J2: "var local j-2"
    steps:
      - name: condiciones
        run: | 
          echo "esto es un saludo ${{ env.CMD }}"
          echo "var local ${{ env.LOCAL_J1 }} "         
          echo "var local ${{ env.LOCAL_J2 }} "         
