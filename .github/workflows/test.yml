name: test

on:
  push:
    branches:
      - main 
env:
  SALUDO: "saludo global"
  CMD: "es un CMD"

jobs:
  j1:
    runs-on: ubuntu-latest
    steps:
      - name: hola desde test
        run: |
         echo "hola bienvenido a test"
         echo "esto es un ${{ env.SALUDO }}"

  j2:
    runs-on: ubuntu-latest
    steps:
      - name: condiciones
        run: echo "esto es un saludo ${{ env.CMD }}"
        
